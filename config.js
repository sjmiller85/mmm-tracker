module.exports = {
  // Require submitted levels to have a score of >= 0
  reqPosScore: true,

  // Require submitters to be twitch subscribers
  subsOnly: false,

  // Whether the queue is open or not
  queueOpen: false,
};
